Apply the `module-foundation` skill for module: $ARGUMENTS

If a target data mode, related-entities list, or explicitly-excluded modules weren't given as part of the arguments, infer them from `BLOOMOS_BIBLE.md`/`docs/*.md` and the most recently migrated sibling module where possible; ask one consolidated question only if genuinely ambiguous. Stop before UI unless explicitly told to include it.
