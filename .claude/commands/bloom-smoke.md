Apply the `live-smoke-test` skill for module: $ARGUMENTS

Report using the permanent QA policy's exact format (Desktop verified / Mobile verified / Chrome: Verified / Safari: Not verified). Only list checks actually performed.
