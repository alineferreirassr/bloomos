Apply the `module-ui` skill for module: $ARGUMENTS

Assume its data-layer foundation already exists unless discovered otherwise. Run `live-smoke-test` for this module before reporting done, in the permanent QA policy's exact reporting format.
