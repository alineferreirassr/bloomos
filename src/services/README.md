# services/

External integration adapters: `email/`, `notifications/`, `payments/`, `storage/`, `weather/`, `ai/`.

Placeholders only. Per the approved architecture, no adapter (not even an interface or mock) is added here until an implemented feature actually needs it — see `docs/integrations.md` for the plan.
