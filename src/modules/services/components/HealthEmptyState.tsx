import { EmptyState } from "@/components/ui/EmptyState";

export function HealthEmptyState() {
  return <EmptyState title="No issues" description="Nothing missing — this Service is fully healthy." />;
}
