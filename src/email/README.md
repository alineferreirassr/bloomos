# email/

Reusable transactional email templates: `welcome-guide/`, `proposal/`, `contract/`, `payment-reminder/`, `feedback/`, `thank-you/`.

Placeholders only. Real template content and the sending mechanism arrive with the Email Center module (`ROADMAP.md` Phase 3) and the `services/email` adapter — not before.
