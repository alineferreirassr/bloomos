# features/

Cross-module features that don't belong to a single MVP domain: `ai/`, `calendar/`, `notifications/`, `email-center/`, `global-search/`.

Placeholders only, reserved for their phase in `ROADMAP.md` (Phase 2-4). No logic lives here during the MVP.
