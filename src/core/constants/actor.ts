/**
 * No authentication exists yet (CLAUDE.md — no fake auth). Every note, note edit,
 * and timeline activity is attributed to this single implicit actor until real
 * Supabase Auth replaces it.
 */
export const CURRENT_ACTOR = "Amoré Bloom Team";
