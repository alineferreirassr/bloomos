# core/

Shared system concerns used across all modules: `permissions/`, `roles/`, `constants/`, `enums/`, `guards/`, `errors/`, `logger/`.

Reserved as of this scaffold. Real content is added starting with the first MVP module (Leads), which needs shared status enums, typed errors, and the route guard — not before.
