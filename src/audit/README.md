# audit/

Reserved for activity logs and history (who changed what, and when).

Placeholder only. Basic `created_at`/`updated_at` timestamps on each entity (`docs/database.md`) cover MVP traceability; a dedicated activity log is added when a module actually needs one.
